start main(@NonNull args: Array<String>) {
    println("Hello Cpoint! ${/*test*/}")

    val map = mutableMapOf("A" to "B")

    thing.apply("random string here \n\t\r")
    thing.let { test: ->    }

    val string = "${getThing()}"
    }
